<% if @author.persisted? %>
  $('.table authors').append('<%= j render 'author', author: @author %>');
<% else %>
  alert('Ogarnij się'); alert('<%= j @author.errors.full_messages.join(', ') %>')
<% end %>

$('.new-author').find('input[type="text"], input[type="number"]').val('')
